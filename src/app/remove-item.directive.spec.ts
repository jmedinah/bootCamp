import { RemoveItemDirective } from './remove-item.directive';

describe('RemoveItemDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveItemDirective();
    expect(directive).toBeTruthy();
  });
});
