import { RemoveVersionPipe } from './remove-version.pipe';

describe('RemoveVersionPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveVersionPipe();
    expect(pipe).toBeTruthy();
  });
});
